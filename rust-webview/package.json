{
  "name": "rust-webview",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch-bundle": "npm run bundle -- --watch",
    "watch-ts": "npm run ts -- --watch",
    "watch-rs": "cargo watch",
    "bundle": "esbuild web/index.ts --bundle --outfile=dist/index.js --platform=browser",
    "ts": "tsc --noEmit",
    "rs": "cargo check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rhysd <lin90162@yahoo.co.jp> (https://rhysd.github.io/)",
  "license": "MIT",
  "dependencies": {
    "github-markdown-css": "^5.1.0",
    "marked": "^4.1.1"
  },
  "devDependencies": {
    "@types/marked": "^4.0.7",
    "esbuild": "^0.15.12",
    "typescript": "^4.8.4"
  }
}
